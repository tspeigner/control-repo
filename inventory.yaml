groups:
  - name: aws_linux
    nodes:
      - linux-01.ts-puppet.vm
      - linux-02.ts-puppet.vm
    config:
      transport: ssh
      ssh:
        user: centos
        private-key: ~/.ssh/tommy.pem
        host-key-check: false
  - name: aws_windows
    nodes:
      - windows-01.ts-puppet.vm
      - windows-02.ts-puppet.vm
    config:
      winrm:
        user: Administrator
        password: Adm1nistrat0r
        ssl: false
